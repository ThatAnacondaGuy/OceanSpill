"""OceanSpill API: case data, analyst actions, authentication, reports and live monitoring.

The web app can run against static artifacts or against this server; both read the same case
files produced by the pipeline, and the server adds shared state, real sign-in and background jobs.
"""
